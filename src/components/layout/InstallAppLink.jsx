export function InstallAppLink() {
  return (
    <p
      style={{
        fontSize: 'var(--font-label)',
        color: 'var(--color-text-muted)',
        textAlign: 'center',
      }}
    >
      Install this app on your iPad: Share → Add to Home Screen
    </p>
  );
}
